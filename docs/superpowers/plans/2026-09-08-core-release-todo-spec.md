# QT 核心研究版本：范围冻结与 TODO spec

## 本轮决策

按用户最新要求停止扩展。以 `/Users/kwt/x/qt` 为唯一主项目，先交付可学习、可编辑、可回测、可复现、可比较的核心研究工作台。未完成工作保留代码和来源，并列入本文件；不继续无限增加模块，不删除原项目研究成果。

“生产级”在此只讨论研究系统的正确性、可复现性和运行可靠性，不代表策略盈利，也不代表实盘、杠杆、强平或逐笔撮合已通过生产验收。

## 冻结的核心范围

1. 本地 OHLCV 数据目录、数据校验和指纹；缺失数据明确拒绝，不补造衍生品/链上数据。
2. 基础策略、原版信号/配置、规则策略和不可变版本；保留来源、公式、参数及生效规则。
3. 已验证的现货 K 线原生回测：因果数据访问、实际订单/成交、手续费、逐笔余额对账、决策解释及结果工件。
4. 单队列后台任务、幂等提交、取消、心跳、失效尝试隔离和任务恢复。
5. Web 学习、策略克隆/编辑/版本、实验、复现、结果比较；同条件比较，不把异数据或异成本结果混排。
6. 本地用户验收入口。Cloudflare 部署配置保留，但未授权、未上线的部分不标完成。

收尾仅允许修复导入/测试错误、恢复本地服务，以及补齐已有核心路径的安全检查。不得借收尾新增交易模式、研究引擎或服务。

## 核心验收证据与限制

- 范围冻结后的最终根项目 Python 回归：**630 通过、8 跳过**（73.65 秒）；Ruff 与 `git diff --check` 通过。
- 独立临时 PostgreSQL 17 补验收：78 通过，包括此前 7 项数据库跳过测试；临时容器已移除。v3 研究工作台本身不依赖 PostgreSQL。
- 共享回测包独立复核：499 通过、6 项外部集成/性能测试未选中。原 RSI 策略和 golden JSON 未修改；测试合成价格改为跨运行环境稳定生成。
- 原生/source 阶段复核：52 通过，包括 S0 入场、止损、退出与非零费用；不是完整杠杆强平验收。
- 前端快照：10 项测试通过，生产构建通过；仍有较大入口包体积警告。
- 实际 OKX 43,800 根小时 K 线：新任务 `712bc45ced074047b1a60afec278548e` 完成，335 笔成交精确对账。原失败记录保留，后续复现和比较已有浏览器证据。
- 该 RSI 验收样例累计收益约 -48.39%、最大回撤约 55.72%；它不是获利推荐。
- Docker 插件已经真实执行到订单、成交和窗口检查；完整隔离验收仍未通过，不能计作核心生产能力。插件结果保持未验证状态。
- 核心最后收尾：Lab 优化/验证统一使用报告发布器；worker 按实际数据目录行数及内存重新检查执行资源。未验证的永续/杠杆请求在 API 与执行器拒绝，历史 market-context 半成品路由已撤下。
- 本地 API、Web 和持续 worker 已用最终代码重启；重启前队列为空，没有中断用户任务。暂未提交或推送 main，Cloudflare 未部署。

## TODO：后续单独立项，不阻塞现货核心学习

| 优先级 | 待办 | 当前边界 | 完成标准 |
| --- | --- | --- | --- |
| P1 | 永续、杠杆、强平与资金费率 | 源策略和部分原生路由已实现；报价/标记价格、保证金盈亏和真实强平链路未完整验收 | 版本化真实报价/标记/费率输入；实际原生强平及费用对账；缺失输入拒绝验证评级 |
| P1 | 完整算法迁移覆盖表 | 50 个原版信号、100 个配置已保留；不能以数量证明所有配置都通过实测 | 每个策略族明确数据要求、可执行入口、同源回归、验证状态；不支持项准确解释 |
| P1 | 多源/SOTA/链上等历史特征 | 原规则和研究代码保留，OHLCV 不足以执行全部多源策略 | 数据版本、公布/修订时间、因果对齐和缺失拒绝；通过原版一致性回归 |
| P1 | 历史 K 线与实际成交标记 | 有决策轨迹和净值；新增 market-context 路径尚需最终验收 | 指纹一致的有界 OHLCV、真实成交工件校验、浏览器点选决策回放；不能静默抽样丢针尖 |
| P1 | Cloudflare 上线 | Worker、Access 校验、私有 R2/源站配置已实现；旧设备授权过期 | 新授权、正确源站容量、端到端鉴权、报告下载、SPA 路由和实际版本验收 |
| P2 | 原版 Qt5 九个工作流的统一 Web 队列入口 | Python 评估/回放入口已有；未全部接到统一 Web 任务 | 9 个原工作流路由→队列→结果测试，区分人工建议与成交回测 |
| P2 | 原版研究门槛统一任务化 | 原版风险、质量、稳健性、晋级等函数保留；未全部队列化 | 类型化参数、原函数执行、可追溯报告与 Web 操作 |
| P2 | 全部具体指标学习目录 | 信号详情已有；具体指标的统一发现与说明需补验收 | 来源命名空间、公式、有效/忽略参数、预热、缺失值和可用时间可查 |
| P2 | 插件隔离最终验收 | 镜像可构建；非 root、只读输入及真实成交已推进 | 实际网络/路由隔离、资源限制、清理/恢复测试；不能仅按网卡名称判断；插件仍不能自证无未来数据 |
| P2 | 实现版本指纹 | 数据/策略版本已固定；执行器实现版本还需更完整绑定 | 改变计算/适配实现后结果身份不会与旧实现混淆，结果保留实现哈希 |
| P2 | 大数据和长任务容量 | 当前单 worker、小规模研究；不扩大到高频基础设施 | 分钟/逐笔任务有明确预算、资源拒绝与恢复；先验收再提高并发 |
| P3 | 体验与包体积优化 | 核心页面可用，入口包较大 | 路由按需加载、指标解释与图表可读性改善，不改变算法语义 |

## 后续执行规则

- TODO 不是已实现承诺；每项再次开始前确认优先级与验收目标。
- 不开通真实交易，不增加实盘资金，不购买数据或服务器。
- Cloudflare 未完成部署时提供本地入口，不用旧版网站或猜测 URL 代替。
- 当前本地测试使用临时数据/状态目录；正式长期使用前迁入持久目录并验证备份恢复。
